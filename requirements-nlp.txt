# NER по русскому тексту. Модели navec/slovnet упакованы внутрь пакета
# (wheel ~34 МБ), скачиваются один раз при сборке образа. В рантайме сеть
# не нужна: инференс идёт локально на CPU.
natasha>=1.6.0
